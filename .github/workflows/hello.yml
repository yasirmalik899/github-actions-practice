name: Test Workflow

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello"

      - name: Print hostname
        run: hostname

      - name: Print current user
        run: whoami
